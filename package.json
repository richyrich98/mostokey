{
  "name": "fractional-video-rights",
  "version": "1.0.0",
  "description": "Tokenize YouTube video rights into ERC-20 fractional units",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:amoy": "npx hardhat run scripts/deploy_factory.ts --network amoy",
    "deploy:sepolia": "npx hardhat run scripts/deploy_factory.ts --network sepolia",
    "test": "npx hardhat test",
    "postinstall": "cd frontend && npm install"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@types/node": "^20.0.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}